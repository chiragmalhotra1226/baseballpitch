# OpenCV MUST come before mediapipe — mediapipe tries to force-install
# opencv-contrib-python (GUI). By pre-installing the headless variant,
# pip sees the opencv requirement as already met and skips the GUI version.
opencv-contrib-python-headless==4.11.0.86

# Core ML stack
mediapipe==0.10.21
numpy==1.26.4

# Streamlit + UI
streamlit>=1.32.0
plotly>=5.18.0
pandas>=2.0.0
Pillow>=10.0.0

# ML models
scikit-learn>=1.3.0
xgboost>=2.0.0
joblib>=1.3.0

# Gemini AI — use google-genai (google-generativeai is EOL as of Nov 2025)
google-genai>=1.5.0

# Utilities
python-dotenv>=1.0.0
protobuf>=4.25.3,<5.0.0